# Heydealer Bid Collector Web

- Entry: `app.py`
- Run: `..\..\run_bid_collector_web.bat` or `python app.py`
- DB: uses shared `..\..\data.db`
- Purpose: collect `내입찰 > 종료(expired)` and save into shared DB with dedup
